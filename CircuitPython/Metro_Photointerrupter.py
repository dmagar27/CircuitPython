import digitalio
import board
import time

interrupt.value = 9

interrupt = digitalio.DigitalInOut(board.D2)
interrupt = digitalio.Direction.INPUT
interrupt = digitalio.Pull.DOWN

counter = 0
time.monotonic(0.1)
while True:
    if interrupt.value
        interrupt = interrupted.value
         counter += 1
    else

